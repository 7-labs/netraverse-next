// Backward-compatible alias for the current content-depth gate.
// The canonical script is validate-word-count.mjs because it uses the same VM loader
// pattern as the static route manifest and works with Next-style extensionless imports.
import './validate-word-count.mjs';
